# Migration history

This folder contains the migrations committed to the repository. It is not guaranteed to contain every schema change: some development changes may be applied directly through the Supabase SQL editor.

When working on a new environment, compare the live Supabase schema with these files before assuming the local migration history is complete.
